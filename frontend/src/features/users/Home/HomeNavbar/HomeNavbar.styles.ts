import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  navBrandTitle: {
    fontSize: '2rem',
    fontFamily: 'Damion',
    color: 'black',
  },
  navBrandLink: {
    textDecoration: 'none',
  },
}));
